# Server properties
server:
  servlet:
    context-path: /gestion-proyectos
  port: 8080

# Spring properties
spring:
  # Dev is active by default. This value can be overwritten by java arg param
  profiles:
    active: heroku
  application:
    name: gestion-proyectos
  messages:
    basename: bundles/apiLanguageBundle
